name: CI (minimal)

on:
  push:
  pull_request:

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: No-op step
        run: echo "CI skipped: no checks for now."
