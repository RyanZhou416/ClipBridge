name: CI (minimal)

on:
  push:
    branches:
      - main
  pull_request:
    branches:
      - main

jobs:
  noop:
    runs-on: ubuntu-latest
    steps:
      - name: Checkout repository
        uses: actions/checkout@v4

      - name: No-op step
        run: |
          echo "CI skipped: no checks for now."
