<Page
    x:Class="ClipBridgeShell_CS.Views.HistoryPage"
	x:Name="HistoryPageRoot"
    xmlns="http://schemas.microsoft.com/winfx/2006/xaml/presentation"
    xmlns:x="http://schemas.microsoft.com/winfx/2006/xaml"
    xmlns:d="http://schemas.microsoft.com/expression/blend/2008"
    xmlns:mc="http://schemas.openxmlformats.org/markup-compatibility/2006"
    xmlns:vm="using:ClipBridgeShell_CS.ViewModels"
    xmlns:models="using:ClipBridgeShell_CS.Core.Models.Events"
    xmlns:l="using:WinUI3Localizer"
    xmlns:helpers="using:ClipBridgeShell_CS.Helpers"
    mc:Ignorable="d">

	<Page.Resources>
		<ResourceDictionary>
			<ResourceDictionary.ThemeDictionaries>
				<ResourceDictionary x:Key="Light">
					<SolidColorBrush x:Key="CardSelectedBorderBrush" Color="Gray"/>
				</ResourceDictionary>
				<ResourceDictionary x:Key="Dark">
					<SolidColorBrush x:Key="CardSelectedBorderBrush" Color="White"/>
				</ResourceDictionary>
			</ResourceDictionary.ThemeDictionaries>
			<helpers:ItemKindConverter x:Key="ItemKindConverter"/>
			<helpers:TimestampConverter x:Key="TimestampConverter"/>
			<helpers:RelativeOrExactTimestampConverter x:Key="RelativeOrExactTimestampConverter"/>
			<MenuFlyout x:Key="HistoryItemMenu"/>
		</ResourceDictionary>
	</Page.Resources>

	<Grid RowDefinitions="Auto,*" Padding="24">

		<Grid Grid.Row="0" ColumnDefinitions="*,Auto,Auto" Margin="0,0,0,16" ColumnSpacing="12">

			<TextBox Grid.Column="0"
                     l:Uids.Uid="HistoryPage_SearchBox"
                     Text="{x:Bind ViewModel.FilterText, Mode=TwoWay, UpdateSourceTrigger=PropertyChanged}" />

			<ComboBox Grid.Column="1" 
                      l:Uids.Uid="HistoryPage_AllTypes"
                      MinWidth="120"
                      SelectedValue="{x:Bind ViewModel.SelectedKind, Mode=TwoWay}"
                      SelectedValuePath="Tag">
				<ComboBoxItem l:Uids.Uid="HistoryPage_AllTypes" Tag="{x:Null}" IsSelected="True"/>
				<ComboBoxItem l:Uids.Uid="HistoryPage_TypeText" Tag="text"/>
				<ComboBoxItem l:Uids.Uid="HistoryPage_TypeImage" Tag="image"/>
				<ComboBoxItem l:Uids.Uid="HistoryPage_TypeFile" Tag="file"/>
			</ComboBox>

			<Button Grid.Column="2" Command="{x:Bind ViewModel.RefreshCommand}">
				<FontIcon Glyph="&#xE72C;" />
			</Button>
		</Grid>

		<ListView Grid.Row="1"
          x:Name="HistoryList"
          ItemsSource="{x:Bind ViewModel.Source, Mode=OneWay}"
          SelectionMode="Single">

			<ListView.ItemsPanel>
				<ItemsPanelTemplate>
					<ItemsStackPanel ItemsUpdatingScrollMode="KeepItemsInView" />
				</ItemsPanelTemplate>
			</ListView.ItemsPanel>

			<ListView.ItemTemplate>
				<DataTemplate x:DataType="models:ItemMetaPayload">
					<Border x:Name="HistoryRowBorder" Tag="HistoryRowBorder"
                  Padding="12"
                  BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
                  BorderThickness="1"
                  CornerRadius="4"
                  Margin="0,4">
						<Grid>
						<Grid.ColumnDefinitions>
							<ColumnDefinition Width="Auto"/>
							<ColumnDefinition Width="*"/>
							<ColumnDefinition Width="Auto"/>
							<ColumnDefinition Width="Auto"/>
							<ColumnDefinition Width="Auto"/>
						</Grid.ColumnDefinitions>

						<FontIcon Glyph="&#xE8C8;"
                          FontSize="24"
                          Margin="0,0,12,0"
                          VerticalAlignment="Center"/>

						<StackPanel Grid.Column="1" VerticalAlignment="Center" Spacing="2">
							<TextBlock Text="{x:Bind Preview.Text, TargetNullValue='(No preview)'}"
                               Style="{ThemeResource BodyStrongTextBlockStyle}"
                               MaxLines="2"
                               TextTrimming="CharacterEllipsis"/>

							<StackPanel Orientation="Horizontal" Spacing="8">
								<TextBlock Text="{x:Bind Kind, Converter={StaticResource ItemKindConverter}}"
                                   Style="{ThemeResource CaptionTextBlockStyle}"
                                   Opacity="0.6"/>
								<TextBlock Text="•"
                                   Style="{ThemeResource CaptionTextBlockStyle}"
                                   Opacity="0.6"/>
								<TextBlock Text="{x:Bind CreatedTsMs, Converter={StaticResource TimestampConverter}}"
                                   Style="{ThemeResource CaptionTextBlockStyle}"
                                   Opacity="0.6"/>
							</StackPanel>
						</StackPanel>

						<Button Grid.Column="2"
								x:Name="HistoryItemLockButton"
								Tag="LockButton"
								VerticalAlignment="Center"
								Margin="4,0,0,0"
								ToolTipService.ToolTip="Lock clipboard"
								Click="LockButton_Click">
							<FontIcon x:Name="HistoryItemLockIcon" Glyph="&#xE785;" FontSize="12"/>
						</Button>

						<Button Grid.Column="3"
								l:Uids.Uid="HistoryPage_CopyButton"
								VerticalAlignment="Center"
								Margin="8,0,0,0"
								Click="CopyButton_Click"/>

						<Button Grid.Column="4"
								x:Name="HistoryItemMoreButton"
								VerticalAlignment="Center"
								Margin="4,0,0,0"
								Flyout="{StaticResource HistoryItemMenu}"
								l:Uids.Uid="HistoryPage_MoreButton">
							<FontIcon Glyph="&#xE712;"/>
						</Button>
					</Grid>
					</Border>
				</DataTemplate>
			</ListView.ItemTemplate>
		</ListView>

		<!-- 浮动 Toast 提示 -->
		<Border x:Name="CopyToast"
				Grid.Row="1"
				VerticalAlignment="Bottom"
				HorizontalAlignment="Center"
				Margin="0,0,0,24"
				Padding="16,10"
				CornerRadius="8"
				Opacity="0"
				IsHitTestVisible="False"
				Background="{ThemeResource AcrylicBackgroundFillColorDefaultBrush}"
				BorderBrush="{ThemeResource CardStrokeColorDefaultBrush}"
				BorderThickness="1"
				Translation="0,0,32">
			<Border.Shadow>
				<ThemeShadow/>
			</Border.Shadow>
			<Border.OpacityTransition>
				<ScalarTransition Duration="0:0:0.25"/>
			</Border.OpacityTransition>
			<Border.TranslationTransition>
				<Vector3Transition Duration="0:0:0.25"/>
			</Border.TranslationTransition>
			<StackPanel Orientation="Horizontal" Spacing="8">
				<FontIcon x:Name="CopyToastIcon"
						  Glyph="&#xE73E;"
						  FontSize="14"/>
				<TextBlock x:Name="CopyToastText"
						   Text="{x:Bind ViewModel.ToastMessage, Mode=OneWay}"
						   VerticalAlignment="Center"
						   FontSize="13"/>
			</StackPanel>
		</Border>

	</Grid>
</Page>
